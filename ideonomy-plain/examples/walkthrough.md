# Walkthrough: deliver the full exploration

Use [the worked example](../references/worked-example.md) as the complete user-visible answer for this request:

> Explore how a neighborhood tool library could broaden access without adding paid staff. Use combination and substitution, and show every combination of three custody arrangements and two access timings.

The answer includes the deliberately selected route, both input sets, all six cells A–F, concise transformation notes for all six candidates, a dependency, a proposed axis revision, an analogy with its limits, and a next investigation. The table enumerates the six-pair space completely; it does not claim to establish feasibility or exhaust every tool-library design.

These are deliverable artifacts, not hidden scratch work. In Codex, put the complete example content in the final answer. A commentary message saying “I explored six options,” a tool log containing the chart, or a final shortlist of C and E would omit the requested output.

For a randomly selected route, run `bin/pick`, read the chosen method bodies, and show the chosen names in the answer before applying them. The picker prints instructions for the exploration; it does not generate the user's exploration. Draws can repeat. If the user requests a follow-up, retain candidate IDs and develop the existing organon instead of automatically drawing again.

For a user-requested summary, it is appropriate to return just the selected directions and their tradeoffs. For an exhaustive enumeration too large to fit inline, preserve the full artifact and disclose exact coverage as specified in [the skill](../SKILL.md#full-visible-output).
